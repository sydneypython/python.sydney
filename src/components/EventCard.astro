---
const { title, date, description, link } = Astro.props;
---

<div class="event-card border rounded-lg p-4 shadow-md">
  <h2 class="text-xl font-bold mb-2">{title}</h2>
  <p class="text-sm text-gray-600 mb-4">{date}</p>
  <p class="text-base text-gray-800 mb-4">{description}</p>
  <a href={link} class="text-blue-500 hover:underline">Read More</a>
</div>